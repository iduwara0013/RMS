'use client';
import { useEffect, useState } from 'react'; import { useRouter } from 'next/navigation';
type User={name:string;roles:string[]}; type App={application_id:number;status:string;candidate:{name:string;nic:string;email:string;phone:string};vacancy:{title:string}}; const API=process.env.NEXT_PUBLIC_API_URL??'http://127.0.0.1:8000/api';
export default function ApplicationsPage(){const router=useRouter();const[user,setUser]=useState<User|null>(null);const[apps,setApps]=useState<App[]>([]);const[msg,setMsg]=useState('');async function load(){const r=await fetch(`${API}/applications`);if(r.ok)setApps((await r.json()).applications??[])}useEffect(()=>{const raw=sessionStorage.getItem('rms_user');if(!raw){router.replace('/');return}setUser(JSON.parse(raw));load()},[router]);async function status(id:number,status:string){if(!user)return;const r=await fetch(`${API}/applications/${id}/status`,{method:'POST',headers:{'Content-Type':'application/json','X-User-Role':user.roles[0]},body:JSON.stringify({status})});const p=await r.json().catch(()=>({}));setMsg(r.ok?p.message:(p.message??'Unable to update application.'));if(r.ok)load()}if(!user)return <main className="dashboard-loading">Loading applications…</main>;return <main className="dashboard-page"><div className="app-wrapper rms-adminlte"><aside className="app-sidebar"><a className="sidebar-brand" href="/dashboard"><img src="/cpstl-logo.png" alt=""/><span><strong>CPSTL RMS</strong><small>Recruitment portal</small></span></a><div className="sidebar-wrapper"><nav><div className="nav-header">Workspace</div><a className="nav-link" href="/dashboard"><span className="nav-icon">⌂</span>Dashboard</a><a className="nav-link" href="/dashboard/vacancies"><span className="nav-icon">＋</span>Vacancies</a><a className="nav-link active" href="/dashboard/applications"><span className="nav-icon">◌</span>Applications</a><a className="nav-link" href="/dashboard/interviews"><span className="nav-icon">▤</span>Interviews</a><a className="nav-link" href="/dashboard/selections"><span className="nav-icon">◆</span>Final selection</a><a className="nav-link" href="/dashboard/notifications"><span className="nav-icon">▣</span>Notifications</a><a className="nav-link" href="/vacancies"><span className="nav-icon">↗</span>Public careers</a><div className="nav-header">Account</div><a className="nav-link" href="#profile"><span className="nav-icon">◉</span>My profile</a></nav></div></aside><section className="app-main"><header className="app-header"><span className="app-header__title">Application management</span><span className="app-header__meta">{user.name}<button className="dashboard-signout" onClick={()=>{sessionStorage.removeItem('rms_user');router.replace('/')}}>Sign out</button></span></header><div className="content-wrapper"><div className="content-header"><div><h2>Applications</h2><p>Verify candidate submissions and prepare the shortlist.</p></div><div className="role-badge"><span className="role-badge__dot"/>{user.roles.join(' · ')}</div></div>{msg&&<p className="form-message form-message--success">{msg}</p>}<div className="applications-list">{apps.map(a=><article className="application-row" key={a.application_id}><div className="application-avatar">{a.candidate.name.charAt(0)}</div><div className="application-info"><div className="application-row__heading"><h3>{a.candidate.name}</h3><span className="workflow-status">{a.status}</span></div><p>Application #{a.application_id} · {a.vacancy.title} · {a.candidate.nic}</p><small>{a.candidate.email} · {a.candidate.phone}</small></div><div className="application-actions">{a.status==='Submitted'&&<><button onClick={()=>status(a.application_id,'Verified')}>Verify</button><button className="application-actions__secondary" onClick={()=>status(a.application_id,'Rejected')}>Reject</button></>}{a.status==='Verified'&&<button onClick={()=>status(a.application_id,'Shortlisted')}>Shortlist</button>}</div></article>)}</div>{apps.length===0&&<div className="vacancy-empty">No applications have been submitted yet.</div>}</div></section></div></main>}
